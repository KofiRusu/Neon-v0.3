{
  "name": "@neon/types",
  "version": "1.0.0",
  "description": "Shared TypeScript type definitions",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "lint": "echo 'No linting needed for types package'",
    "typecheck": "tsc --noEmit",
    "test": "echo 'No tests for types package'",
    "test:ci": "echo 'No tests for types package'"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "exports": {
    ".": "./src/index.ts"
  }
}
